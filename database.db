import sqlite3

conn = sqlite3.connect('database.db')
cursor = conn.cursor()

cursor.execute('''
CREATE TABLE IF NOT EXISTS usuarios (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    email TEXT NOT NULL UNIQUE,
    senha TEXT NOT NULL
)
''')

# Inserir um usuário teste
cursor.execute("INSERT INTO usuarios (email, senha) VALUES (?, ?)", ('admin@email.com', '1234'))

conn.commit()
conn.close()